# How-To: Pact Contract Tests

1. Add `PactNet` to test project.
2. See `tests/ContractTests.Pact/*` for examples.
3. Generate pact files → publish to broker or commit artifacts for provider verification.
